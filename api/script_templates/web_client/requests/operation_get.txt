
await get{{MODEL_NAME}}(id, session.token).then(async (response)=>{
    if (response.status === 200){
        set{{MODEL_FIELD_NAME_TITLE}}
    }else{
        setAlertType('Error');
        setAlertMessage('Error al obtener los datos.')
        setError(true);
    }
});