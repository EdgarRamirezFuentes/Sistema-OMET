import * as React from 'react';
import { createStackNavigator } from '@react-navigation/stack';
{{SCREEN_IMPORTS}}
export default function {{APP_MODEL_NAME_TITLE}}() {
    const Stack = createStackNavigator();
    return (

        <Stack.Navigator initialRouteName="{{APP_MODEL_NAME_TITLE}}ListView">
            {{STACK_SCREEN_ITEMS}}
        </Stack.Navigator>
    );
}